- 2026-05-01T00:00:00.000Z TillDone durable state was introduced with Drizzle table definitions plus startup schema repair, so local databases can support runner/worker status before scheduler behavior changes.
- 2026-05-01T00:00:00.000Z TillDone task claiming now selects the runnable task and compatible agent inside one Todo transaction, preventing scheduler slots from racing over task selection.
- 2026-05-01T00:00:00.000Z TillDone scheduling moved from per-agent loops to reusable worker slots, so free capacity can immediately claim new runnable work while other slots keep running.
- 2026-05-01T00:00:00.000Z TillDone runner status now reads from durable runner and worker rows while runtime memory only tracks active flags and fibers, making status visible beyond the in-memory scheduler loop.
- 2026-05-01T00:00:00.000Z TillDone graceful stop now lets active worker prompts finish and records their summaries before ending, while preventing newly queued work from being claimed after stop is requested.
- 2026-05-01T00:00:00.000Z TillDone abort now treats terminal runner states as idempotent and marks active durable worker rows aborted so hard stops do not leave phantom active slots.
- 2026-05-01T00:00:00.000Z TillDone start now distinguishes live in-process runners from stale durable state, interrupting only stale active workers/tasks during recovery before launching a fresh scheduler.
- 2026-05-01T00:00:00.000Z TillDone runner tests now assert scheduling behavior through recorded prompt agents, session usage, timing, and cancellation calls instead of relying only on final task states.
- 2026-05-01T00:00:00.000Z The Done-section clear action now uses a Lucide trash glyph, keeping TillDone controls visually consistent with icon-only actions.
- 2026-05-01T00:00:00.000Z TillDone worker results now record the specific task outcome path, preserving child session ids and concise summaries while avoiding broad agent-level interruption for a single failed task.
- 2026-05-01T00:00:00.000Z The TillDone sidebar now renders durable worker rows and timestamp/error state from the status API, with controls shown only for states where Start, Stop, or Abort are valid.
- 2026-05-01T00:00:00.000Z The Knowledge area was reframed as a Diary view that refreshes on tab selection and opens truncated timeline entries in a focused modal.
- 2026-05-01T00:00:00.000Z Cron scheduling is planned as a queue-producing feature rather than a worker executor, keeping scheduled runs compatible with the TillDone worker model and diary logging.
- 2026-05-01T00:00:00.000Z QA-owned TillDone work now stays visually blue only while it is pending or active, returning to normal completion colors once it leaves QA.
- 2026-05-01T00:00:00.000Z QA/dev handoffs now mutate the same TillDone task row instead of spawning replacements, preserving one task id with an ASCII history timeline.
- 2026-05-01T00:00:00.000Z Focused fixture tests now isolate project ids per temp workspace and avoid global instance disposal, preventing concurrent TillDone/taskqueue tests from sharing queue state.
- 2026-05-01T00:00:00.000Z Aborted TillDone runs now finalize through the normal finish-status path, turning interrupted/open work into a blocked terminal state so users can start the runner again.
- 2026-05-01T02:56:46.774Z Task completion now remains completed and no longer creates/reassigns QA automatically. QA is manual: users can request it explicitly or generate context via the new `qa-prep` command.
- 2026-05-01T03:29:43.507Z Sidebar resizing uses persisted `sidebar_width` with shared clamping; narrow split keeps main-feed space while overlay mode clamps to viewport width. Plugin SDK would need richer sidebar/tab/layout/status/navigation hooks beyond the existing slots to support the current TillDone/Diary/Kanban experience without core TUI changes.
- 2026-05-01T03:30:00.000Z Task mode is implemented as a native primary agent with strict queue-only permissions and prompt-level behavior, while taskqueue enforces plan/task maintenance semantics for pending tasks only. The SDK proposal separates reusable upstream API/plugin chunks from fork-specific TillDone branding and local UI polish.
- 2026-05-01T03:34:00.000Z Sidebar resize drags now use a route-level temporary mouse capture layer because the one-cell OpenTUI grip stops receiving drag events once the pointer leaves it. Diary sidebar remains compact at five entries per day while the full sorted day opens in a timeline modal.
- 2026-05-01T04:04:31.032Z Used a route-level temporary mouse capture layer for sidebar resize drags because the one-cell OpenTUI grip stops receiving drag events once the pointer leaves it. Kept Diary sidebar compact at five entries per day while passing the full sorted day into the timeline modal.
