- 2026-04-30T18:54:50.775Z Added Drizzle table definitions for `tilldone_runner` and `tilldone_worker`. Added startup schema repair using `CREATE TABLE IF NOT EXISTS` / `CREATE INDEX IF NOT EXISTS` so local DBs get durable TillDone state even when migrations are unavailable.
- 2026-04-30T18:57:10.074Z Added `Todo.claimNextRunnable({ sessionID, agents })` with task selection and agent assignment inside the Todo transaction. Allowed agent entries to be strings or `{ name, hidden }` so hidden agents can be filtered at the claim API boundary.
- 2026-04-30T19:00:29.074Z Replaced per-agent loops with fixed worker slot loops keyed by slot index. Each free slot now calls `Todo.claimNextRunnable` against the full visible subagent set, so slots are not tied to specific agents.
- 2026-04-30T19:04:43.104Z Durable runner/worker tables are now the source for `status()`. Runtime memory still only tracks active runner flags/fibers.
- 2026-04-30T19:06:37.262Z Preserved worker completion summaries by adding optional `summary` support to `Todo.complete`, appending it to task history. Added focused graceful stop coverage that verifies stop enters `stopping`, active work completes, new post-stop work remains pending, final status becomes `blocked`, and worker summary history is retained.
- 2026-04-30T19:09:09.681Z Treat `complete`, `blocked`, and `error` as terminal statuses for abort idempotency. Preserve durable worker records while marking active worker slots `aborted` so status no longer reports them active.
- 2026-04-30T19:11:18.028Z Existing in-process recoverable runners now short-circuit `start`, so live workers are not treated as stale. Durable stale `running` worker rows are marked `interrupted` with recovery error metadata before starting a fresh runner.
- 2026-04-30T19:14:55.551Z Extended the fake prompt layer to record agent/session/timing and cancellation calls so runner behavior can be asserted directly. Cleaned up nonterminal test tasks after stop/abort/recovery cases to avoid cross-test durable queue pollution.
- 2026-04-30T19:16:47.586Z Replaced the existing emoji trash label with the Lucide trash icon glyph (`\ue18d`) for the Done-section clear button.
- 2026-04-30T19:21:35.288Z Added explicit task lifecycle paths for blocked and interrupted worker results so the runner records the specific task instead of interrupting all active tasks for an agent. Worker completion history now stores the child session id and a concise summary section rather than the full report.
- 2026-04-30T19:23:19.369Z Kept the existing durable TillDone status API shape because it already exposes runner state, active/max workers, timestamps, errors, and worker rows. Updated the TUI to render active workers, stopped/completed time, error/interrupted worker state, and the required Start/Stop/Abort visibility rules.
- 2026-04-30T19:24:56.919Z Regenerated SDK because TillDone API response schema changed. No SDK client method changes were produced; only generated response types changed.
- 2026-04-30T19:26:35.511Z Cron scheduler only enqueues normal TillDone tasks; agent workers remain responsible for claiming/execution. Cron UI is placed between `Todo` and `Done`, with active rows/header using yellow `theme.warning`.
- 2026-04-30T19:26:36.852Z Renamed the visible Knowledge tab label to `Diary` to match the requested diary-tab behavior. Diary tab clicks now refetch diary entries.
- 2026-04-30T19:27:52.608Z QA-assigned tasks now render blue while pending or in progress. Completed/cancelled tasks stay muted so they return to the normal done-state coloring instead of remaining QA-blue.
- 2026-04-30T19:30:17.641Z Added a `Todo.handoff` lifecycle path so QA/dev handoffs reuse the same task row/id instead of creating replacement tasks. Taskqueue output now renders task history as an ASCII timeline when history exists.
- 2026-04-30T19:31:02.777Z Did not delete `.opencode/references/effect-smol` because it is intentionally referenced by the Effect skill.
- 2026-04-30T19:41:09.156Z Isolated non-git temp fixture projects by writing a per-temp `.git/opencode` project id so concurrent fixture tests no longer share the global project todo queue. Removed broad `Instance.disposeAll()` afterEach cleanup from the focused fixture tests; `provideTmpdirInstance` already disposes its own instance and global disposal was unsafe across concurrent test files.
- 2026-04-30T19:48:35.503Z Added a separate focused graceful-stop test for the `complete` finish path without altering the existing blocked-path coverage.
- 2026-04-30T19:59:52.048Z Finalize `aborting` runners through the existing `finishStatus(...)` path so interrupted/open work becomes `blocked`, restoring Start controls. Made the hard-abort test wait for the worker prompt to start before aborting, avoiding a race where abort happens before worker state exists.
- 2026-04-30T20:05:07.246Z Added small exported diary helpers so the focused package test can cover refresh/list formatting behavior without introducing a full TUI renderer harness. Reused those helpers in the sidebar so the coverage tracks the live sidebar behavior.
- 2026-04-30T20:14:23.423Z Renamed the diary test file to `.tsx` so it can render TUI components directly. Exported focused Diary UI components needed for component-level coverage.
